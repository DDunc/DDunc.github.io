$(document).ready(function(){

	var quizQ = [];
	function QuestionMake(qtext){
		this.qtext = qtext;
		}
		quizQ.push(this)
	}

	var question1 = ["Go Limp", "Do a flying triangle", "Tap out", "Hip escape (correct)"]

	})

	if (item.indexOf("(correct)") > -1){
		item.replace("(correct)", "");
	}
	$("")



})